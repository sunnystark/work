import React from 'react';
import styled from 'styled-components';
import { Button } from 'semantic-ui-react';

export const ClearAllBtn = styled.button` .ui.button{
       border: 1px grey;
    background-color: white;
     border-radius: '4px';
}`;